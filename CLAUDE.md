# CLAUDE.md — 프로젝트 작업 지침

이 파일은 Claude Code가 매 세션 자동으로 읽는다. 프로젝트의 헌법이다.

---

## 프로젝트

**웬델: 마석탑의 수문장** — 라인 방어형 오토배틀러 + 캐릭터 수집 + 로그라이트
1인 개발. Godot 4.x / GDScript. 2D 가로(1280x720). 모바일 메인.

전체 기획: `GDD.md`
현재 범위: `docs/CURRENT_PHASE.md` ← **작업 시작 전 반드시 확인**

---

## 역할 분담 (가장 중요)

- **`.tscn` 씬 파일은 사용자가 Godot 에디터에서 직접 만든다.**
  Claude는 씬 파일을 텍스트로 생성하거나 편집하지 않는다.
  노드 ID·리소스 참조가 얽힌 포맷이라 텍스트 편집 시 씬이 깨진다.
- **Claude는 `.gd` 스크립트와 `.json` 데이터만 작성한다.**
- 씬 구조가 필요하면 **클릭 단위로 안내한다.** 예:
  ```
  1. Scene → New Scene → Node2D 선택
  2. 이름을 Unit으로 변경
  3. Unit 우클릭 → Add Child Node → Sprite2D
  4. Unit 선택 → 스크립트 아이콘 → scripts/combat/unit.gd 연결
  5. Ctrl+S → scenes/unit.tscn 저장
  ```
- 사용자는 Godot 초보다. 전문 용어를 쓸 때는 짧게 설명을 붙인다.

---

## 절대 규칙

### 1. 범위를 넘지 않는다
`docs/CURRENT_PHASE.md`에 없는 기능은 **구현하지 않는다.**
좋은 아이디어는 코드가 아니라 `docs/BACKLOG.md`에 적는다.

### 2. 데이터는 코드에 넣지 않는다
가신 스탯, 스킬 수치, 층 구성, 대사는 **전부 `data/*.json`**.
새 가신 추가가 JSON 한 블록이 되도록 만든다.

### 3. 세이브 데이터는 함부로 바꾸지 않는다
`docs/SAVE_FORMAT.md` 스키마를 따른다. 변경 시 마이그레이션 코드 필수.
**여기 버그는 유저 데이터를 날리고 복구가 안 된다.**

### 4. 한 번에 하나씩
여러 시스템을 동시에 건드리지 않는다. 하나 만들고 실행 확인하고 다음.

### 5. 불확실하면 물어본다
설계 의도가 불분명하면 추측해서 구현하지 말고 질문한다.

---

## 게임 구조 핵심

### 라인 방어형
```
[아이다]  [전열]  [중열]  [후열]   ←←← 적 진군
  좌측 고정        슬롯 배치
```

- **적은 오른쪽에서 왼쪽으로만 이동한다.** 경로탐색 없음
- **가신은 고정 슬롯에서 이동하지 않는다.** 방향 전환도 없다
- 적이 좌측 끝에 닿으면 아이다 피해 → HP 0이면 실패
- 아이다도 이동하지 않는다. 조작은 **버튼 3개뿐**

### 유닛은 걷지 않는다
스프라이트는 대기 2프레임 + 공격 2프레임, 총 4프레임.
이동/방향 전환 애니메이션을 요구하는 설계를 하지 않는다.

### 화려함은 이펙트가 만든다
캐릭터 모션이 아니라 파티클·셰이더·화면 연출로 화면을 채운다.
**이건 코드 영역이다.** 아트 리소스를 늘리지 않고 임팩트를 올리는 유일한 방법.

### 스킬 = 컷인
버튼을 누르면 상반신 일러가 슬라이드 인 + 대사 + 플래시. 0.6초.
전투는 멈추지 않는다. 정지 이미지 + 화면 연출만 사용. 애니메이션 없음.

---

## 코드 컨벤션

```gdscript
# 파일명: snake_case.gd / 클래스: PascalCase
# 함수·변수: snake_case / 상수: SCREAMING_SNAKE_CASE
# private: _언더스코어 접두

func apply_damage(amount: float, source: Node2D) -> void:   # 타입 힌트 항상
signal boss_defeated(hero_id: String)                        # 시그널은 과거형
```

### 디렉토리
```
scenes/     .tscn (사용자가 생성)
scripts/
  /core     게임 루프, 상태 관리, 오브젝트 풀
  /combat   유닛, 적, 투사체, 데미지
  /ui       HUD, 편성, 컷인
  /data     JSON 로더
data/       .json
assets/     이미지, 사운드
docs/       설계 문서
```

---

## 성능

라인 방어형은 뱀서라이크보다 부담이 낮지만 모바일 기준은 지킨다.

- **오브젝트 풀 필수** — 적, 투사체, 이펙트, 데미지 숫자
- 적 AI는 좌측 직진뿐. 경로탐색 금지
- 충돌은 단순 원형/사각 판정
- 목표: **적 100마리 + 이펙트에서 60fps**

---

## 하지 말 것

- ❌ `.tscn` 파일 직접 생성/편집
- ❌ 멀티플레이 / 서버 / 리더보드
- ❌ 유닛 이동·경로탐색·방향 전환
- ❌ 액션 조작 (수동 공격, 회피)
- ❌ 3D
- ❌ 확률형 유료 아이템 코드
- ❌ 외부 API 호출
- ❌ 요청하지 않은 리팩토링
- ❌ 500줄 넘는 단일 스크립트
