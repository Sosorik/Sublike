# CLAUDE.md — 프로젝트 작업 지침

이 파일은 Claude Code가 매 세션 자동으로 읽는다. 프로젝트의 헌법이다.

---

## 프로젝트

**웬델: 마석탑의 수문장** — 뱀서라이크 로그라이트 + 캐릭터 수집
1인 개발. Godot 4.x / GDScript. 2D. 모바일 + Steam.

전체 기획: `GDD.md`
현재 작업 범위: `docs/CURRENT_PHASE.md` ← **작업 시작 전 반드시 확인**

---

## 절대 규칙

### 1. 범위를 넘지 않는다
`docs/CURRENT_PHASE.md`에 없는 기능은 **구현하지 않는다.**
좋은 아이디어가 떠오르면 코드로 쓰지 말고 `docs/BACKLOG.md`에 적는다.

### 2. 데이터는 코드에 넣지 않는다
캐릭터 스탯, 무기 수치, 층 구성, 대사는 **전부 `data/*.json`**.
GDScript에 숫자를 하드코딩하면 밸런싱이 불가능해진다.
새 캐릭터 추가가 JSON 한 줄이 되도록 만든다.

### 3. 세이브 데이터는 함부로 바꾸지 않는다
`docs/SAVE_FORMAT.md`의 스키마를 따른다.
스키마 변경 시 반드시 마이그레이션 코드를 함께 작성한다.
**여기 버그가 나면 유저 데이터가 날아가고 복구가 안 된다.**

### 4. 한 번에 하나씩
여러 시스템을 동시에 건드리지 않는다. 하나 만들고, 실행해서 확인하고, 다음으로.

### 5. 확실하지 않으면 물어본다
설계 의도가 불분명한 부분은 추측해서 구현하지 말고 질문한다.

### 6. 씬 파일(.tscn)은 건드리지 않는다
**`.tscn` / `.tres` 등 씬·리소스 파일은 개발자가 Godot 에디터에서 직접 만든다.**
Claude는 **`.gd` 스크립트만 작성한다.**

- `.tscn` 파일을 텍스트로 생성·편집·수정하지 않는다. 읽는 것은 가능하다.
- 씬 구조가 필요하면 코드로 만들지 말고, **에디터에서 어떻게 배치할지 매우 상세하게 설명한다.**
  - 노드 트리 전체 (부모-자식 관계, 들여쓰기로 표현)
  - 각 노드의 **정확한 노드 타입**과 **이름** (스크립트의 `$경로`와 반드시 일치)
  - 인스펙터에서 설정할 값 (position, scale, collision_layer/mask, 텍스처, 앵커 등)
  - 어느 노드에 어느 `.gd`를 붙이는지
  - 시그널 연결이 필요하면 어느 노드의 어느 시그널을 어디에 연결하는지
  - 오토로드 등록이 필요하면 이름과 경로
- 스크립트는 **설명한 노드 구조를 전제로** 작성한다. 노드 이름이 바뀌면 스크립트가 깨지므로 이름을 임의로 바꾸지 않는다.
- 예외: 개발자가 명시적으로 "이 씬 파일을 직접 수정해라"라고 요청한 경우.

---

## 아키텍처 원칙

### 데이터 주도 설계
```
data/heroes.json    → 캐릭터 정의
data/weapons.json   → 무기 정의
data/skills.json    → 아이다 스킬 정의
data/floors.json    → 층/구간 정의
data/dialogue.json  → 대사
```
런타임에 로드해서 조립. 코드는 "해석기"만 담당한다.

### 무기는 캐릭터에 종속되지 않는다
캐릭터가 무기를 **참조**한다. 여러 캐릭터가 같은 무기를 공유하고,
패시브로 차별화한다. (40명 ≠ 40무기. 무기는 15~20종)

### 전투는 완전 자동
플레이어는 아이다만 조작한다. 이동 + 액티브 버튼 3개.
**소녀들의 공격 로직에 플레이어 입력이 개입하지 않는다.**

### 서버 없음
전부 로컬. 네트워크 코드를 작성하지 않는다.

---

## 코드 컨벤션

```gdscript
# 파일명: snake_case.gd
# 클래스: PascalCase
# 함수/변수: snake_case
# 상수: SCREAMING_SNAKE_CASE
# private: _언더스코어 접두

# 타입 힌트를 항상 쓴다
func apply_damage(amount: float, source: Node2D) -> void:

# 시그널은 과거형
signal boss_defeated(hero_id: String)
```

### 디렉토리
```
scenes/     .tscn
scripts/    .gd
  /core     게임 루프, 상태 관리
  /combat   전투, 무기, 투사체
  /ui       UI
  /data     JSON 로더, 데이터 모델
data/       .json
assets/     이미지, 사운드
docs/       설계 문서
```

---

## 성능 (모바일 필수)

뱀서라이크는 화면에 적이 수백 마리 나온다. 처음부터 지켜야 한다.

- **오브젝트 풀링 필수** — 적, 투사체, 이펙트. `instantiate()`/`queue_free()` 반복 금지
- 적 AI는 단순하게. 경로탐색 쓰지 않는다. 플레이어 방향으로 직진
- 물리 레이어 최소화. 충돌 판정은 단순 원형
- 목표: **적 300마리 동시 표시에서 60fps**

---

## 작업 흐름

```
1. docs/CURRENT_PHASE.md 확인
2. 작업 대상 파일 읽기
3. 구현
4. godot --headless로 파싱 에러 확인
5. docs/PROGRESS.md에 완료 항목 기록
```

---

## 하지 말 것

- ❌ 멀티플레이 / 서버 / 리더보드
- ❌ 액션 조작 (수동 공격, 회피, 콤보)
- ❌ 3D
- ❌ 확률형 유료 아이템 관련 코드
- ❌ 외부 API 호출
- ❌ 요청하지 않은 리팩토링
- ❌ `.tscn` 씬 파일 직접 생성/편집 (→ 절대 규칙 6)
- ❌ 500줄 넘는 단일 스크립트 (분리할 것)
